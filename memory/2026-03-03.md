# 2026-03-03

## S4S Debugging Session

### Lindsay Cahill / Alizabeth Issue
- Kiefer reported model Lindsay Cahill complaining S4S broken
- Root cause: lindsaycahill had vault mappings in only 1/70 targets (nearly zero promotion coverage)
- Also found: katievancee missing from 66 targets, taylorskully had 0 models with her photo
- Removed all three via `/api/remove-model` — Kiefer needs to re-add through dashboard

### Delete Backlog Fix
- Delete queue was 542 deep (should be ~30)
- Two fixes: parallel batch deletes (20 at once) + treat OF API 500 "Server Error" on already-deleted posts as success
- Deployed to Railway, backlog cleared

### Vault Health Safeguards Built
- `/api/vault-health/route.ts` — audit endpoint
- `/api/distribute-retry/route.ts` — retry failed distributions
- Post-distribute validation in add-model page (red warning + retry button)
- Promo image saved to KV (`promo_image:{username}`) during distribute
- Vault health check hooked into midnight schedule regen — unhealthy models (<90%) auto-skipped
- `vaultHealthAlert` added to `/stats` endpoint

### Promo Image CDN Limitation
- OF CDN URLs are IP-restricted (CloudFront signed with IP policy)
- Cannot copy promo images server-side — only exist in browser during upload
- Storing images in KV for future use

### Dashboard Fixes (Completed)
- "Check Vault Gaps" now uses server-side KV (`/api/vault-gaps`) instead of browser IndexedDB
- Gaps UI grouped by **promoter** instead of target — much clearer
- "Fix All Gaps" had inverted source/target — fixed imageSourceToVaults mapping
- "Fix All Gaps" speed fix — removed CHUNK_SIZE=3, sends all targets in one `/api/distribute` call
- Distribute endpoint had backwards KV writes: was `mappings[imageOwner][vaultOwner]`, fixed to `mappings[promoter(vaultOwner)][target(imageOwner)]`
- Vault count badge now filters to connected models only (was showing 72/69)
- "Force Redistribute to all X" button added on model page

### OF API Delete Endpoint BROKEN (Critical)
- DELETE `/api/{account}/posts/{post_id}` returns 500 "Server Error" for ALL accounts
- Posts remain after 500 — verified via GET
- **Previous "fix" was WRONG**: treating 500 as success just cleared queue without deleting posts — the backlog "clearing" from 542→30 was fake
- Every model has 20-25+ undeleted ghost tags sitting on their profiles
- **Rotation service STOPPED** + pinned posts disabled to prevent creating more ghost tags
- Reverted deletePost() to do GET verification after 500, but NOT YET DEPLOYED to Railway
- Need to resolve with OF API support or find workaround before restarting

### Promotion Balancing (In Progress)
- Current algorithm: random shuffle + modulo = unequal promotion distribution
- Models with fewer promoters get fewer total promotions
- Need global target counting across all promoters to equalize exposure

### Current State
- Rotation service: **STOPPED** (prevent more ghost tags)
- 199 vault gaps: lindsaycahill (67 as promoter), katievancee (65 as promoter), 67 others missing taylorskully
- lindsaycahill re-added by Kiefer (69 models have her photo as target ✅, only 2 as promoter)
- katievancee re-added by Kiefer (72 as target ✅, only 5 as promoter)
- taylorskully still PROMOTER_ONLY per Kiefer
- Bianca daemon: running but disabled since Feb 25

### Key Locations
- PROMOTER_ONLY set at line 746 of index.js: `new Set(['taylorskully'])`
- taylorskully = promotes others but nobody promotes her (hardcoded)
- 4 disconnected models in vault: zoeygraceee, oliviabrookess, celestinevance, diordelilah
